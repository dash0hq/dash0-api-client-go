# Allowed incompatible API changes.
# Each line is a pattern matched against the symbol name (shell glob syntax).
# Lines starting with # are comments.
# Clear this file after tagging a release.
#
# Note: interface method additions (": added") are filtered out automatically
# and do not need to be listed here.

# v1.6.0: CheckThresholds fields changed from *float32 to *float64 (upstream
# OpenAPI spec switched from float to double).
CheckThresholds.Degraded
CheckThresholds.Failed

# v1.6.0: PrometheusAlertRule.Annotations changed from *map[string]string to
# the typed *PrometheusAlertRule_Annotations struct (upstream spec change).
PrometheusAlertRule.Annotations

# v1.6.0: PrometheusAlertRule.KeepFiringFor changed from *string (camelCase
# keepFiringFor) to *Duration (snake_case keep_firing_for). The deprecated
# camelCase field is removed by tools/postprocess because oapi-codegen generates
# both as the same Go field name, causing a compilation error.
PrometheusAlertRule.KeepFiringFor

# v1.6.0: PrometheusAlertRule.Description and .Summary removed by
# tools/postprocess. The upstream spec deprecated them in favor of the
# Annotations.Description and Annotations.Summary fields.
PrometheusAlertRule.Description
PrometheusAlertRule.Summary

# v1.9.0: Recording rule group API removed (proprietary endpoint
# replaced by the standard PrometheusRule-based recording rules endpoint).
Client.*RecordingRuleGroup*
(*MockClient).*RecordingRuleGroup*
MockClient.*RecordingRuleGroup*
(*ClientWithResponses).*RecordingRuleGroup*
(*generatedClient).*RecordingRuleGroup*
ClientInterface.*RecordingRuleGroup*
ClientWithResponsesInterface.*RecordingRuleGroup*
Dash0RecordingRuleGroup
RecordingRuleGroup*
RecordingRule
RecordingRuleSource*
*ApiRecordingRuleGroups*

# v1.12.1: Upstream OpenAPI spec wrapped spam-filter request/response
# bodies in union types (SpamFilterCreateRequest, SpamFilterResponse) that
# carry v1alpha1 and v1alpha2 alternates. The Go signatures printed by
# gorelease look identical but the underlying type alias targets changed,
# so every spam-filter symbol is flagged. The SDK preserves the v1alpha1
# shape externally (see client_spam_filters.go).
SpamFilter
SpamFilter*
*SpamFilter*
Client.*SpamFilter*
(*ClientWithResponses).*SpamFilter*
(*generatedClient).*SpamFilter*
ClientInterface.*SpamFilter*
ClientWithResponsesInterface.*SpamFilter*
NewPostApiSpamFiltersRequest
NewPutApiSpamFiltersOriginOrIdRequest
GetApiSpamFiltersOriginOrIdResponse.JSON200
PostApiSpamFiltersResponse.JSON200
PutApiSpamFiltersOriginOrIdResponse.JSON200
GetSpamFiltersResponse.SpamFilters
GetSpamFilterDataset
GetSpamFilterID
GetSpamFilterName
SetSpamFilterDataset
SetSpamFilterID
SetSpamFilterIDIfAbsent
ClearSpamFilterID
StripSpamFilterServerFields

# v1.12.1: TelemetryFilterContextMetric and TelemetryFilterContextSpanEvent
# constants were removed upstream (no longer valid spam-filter contexts in
# the latest spec). Consumers cannot reference them anyway because they
# never matched a server-accepted value.
TelemetryFilterContextMetric
TelemetryFilterContextSpanEvent

# v1.12.3: Upstream OpenAPI spec moved the dash0.com/source field on
# dashboards out of DashboardAnnotations and into the new DashboardLabels
# struct, reachable via DashboardMetadata.Labels.Dash0Comsource. Read and
# write call sites must move from .Annotations.Dash0Comsource to
# .Labels.Dash0Comsource. There is no field-level shim because Go does not
# support aliasing struct fields.
DashboardAnnotations.Dash0Comsource

# v1.13.1: DeeplinkURL and ViewDeeplinkURL gained a trailing
# `dataset *string` parameter so links can be scoped to a specific dataset
# in the Dash0 web app. Pass nil for the previous behavior (no dataset
# query parameter). The deep-link builders were introduced one release
# earlier in v1.13.0; there is no function-level shim because Go does not
# support overloading and an alias cannot change arity.
DeeplinkURL
ViewDeeplinkURL

# v1.13.1: Upstream OpenAPI spec dropped the
# externalRedMetricsLastSeenAt field on DatasetMetricsSettings. Consumers
# reading it should fall back to absence-as-unknown.
DatasetMetricsSettings.ExternalRedMetricsLastSeenAt

# v1.16.0: Upstream OpenAPI spec removed the ResponseFormat type
# (with its ResponseFormatJson and ResponseFormatYaml constants) and the
# `format` query parameter on the SLO GET endpoint
# (GetApiSlosOriginOrIdParams.Format). No in-package shim is possible
# because the type, constants, and field were removed wholesale by the
# upstream spec.
ResponseFormat
ResponseFormatJson
ResponseFormatYaml
GetApiSlosOriginOrIdParams.Format

# v1.17.0: The team CRD envelope is now exposed as
# TeamDefinitionV1Alpha1 (renamed in postprocess) so the API-version
# suffix is visible in every call site. `TeamDefinition` remains as a
# deprecated type alias in client_teams.go so v1.x consumers can migrate
# incrementally, but the generated struct is exported under the versioned
# name and downstream generated symbols (JSON200 fields, request bodies,
# and so on) point at the new type identity.
TeamDefinitionKind
PostApiTeamsJSONRequestBody
PostApiTeamsResponse.JSON200
GetApiTeamsResponse.JSON200

# v1.17.0: The upstream spec now exposes SpamFilterApiVersion as
# a top-level union type covering both v1alpha1 and v1alpha2, and
# SpamFilterApiVersionV1Alpha1 stays generated as the narrower type. The
# deprecated `V1alpha1` alias in compat.go now points at the wider
# enum's `SpamFilterApiVersionV1alpha1` value.
V1alpha1

# v1.17.0: Client.GetTeam now returns *TeamDefinitionV1Alpha1
# instead of *GetTeamResponse; the CRD envelope replaces the enriched
# UI-only response. The client extracts the envelope from the server's
# enriched response under the hood so callers work with the canonical
# type. GetTeamResponse itself remains generated for callers that need
# the raw enriched shape, and is exposed via the new GetTeamWithAssets
# method.
Client.GetTeam
(*MockClient).GetTeam
MockClient.GetTeamFunc
Client.CreateTeam
(*MockClient).CreateTeam
MockClient.CreateTeamFunc

# v1.17.0: TeamLabels gained an AdditionalProperties map so that
# arbitrary user-supplied keys survive JSON round-trips (the server
# accepts them on write but silently drops any key outside dash0.com/*).
# Adding the map field makes the struct non-comparable, which is a wire
# non-issue but shows up as a gorelease flag.
TeamLabels

# v1.17.0: The team GET response schema on the public /api/ surface
# now returns the enriched GetTeamResponse (envelope under .Team plus
# accessible-asset arrays and enriched member records). Fields on
# GetTeamResponse are re-typed accordingly (Team is TeamDefinitionV1Alpha1
# rather than TeamDefinition).
GetTeamResponse
GetTeamResponse.Team
GetTeamResponse.Members
GetTeamResponse.Dashboards
GetTeamResponse.CheckRules
GetTeamResponse.SyntheticChecks
GetTeamResponse.Views
GetTeamResponse.Datasets
GetApiTeamsOriginOrIdResponse.JSON200

# v1.17.0: NotificationChannelType, SamplingMode, and the retention
# duration enums were renamed by oapi-codegen from short forms (Slack,
# Webhook, SamplingModeAdaptive, N1d, N7d, ...) to fully-qualified type-
# prefixed names (NotificationChannelTypeSlack, SamplingModeAdaptive
# remains but the bare Adaptive alias is gone, RetentionPeriodN1d, ...).
# Every downstream Dash0 IaC tool references these enums through the
# type-prefixed names already, so no shims are needed. The bare short
# names remained as accidental exports until the upstream spec regen
# tightened the schema.
AllQuiet
Betterstack
DiscordWebhook
Email
EmailV2
GoogleChatWebhook
Ilert
Incidentio
JiraServiceManagementOps
Opsgenie
Pagerduty
PrometheusAlertmanager
PrometheusWebhook
Slack
SlackBot
TeamsWebhook
Webhook
SamplingModeAdaptive
SamplingModeDisabled
Default
Forever
N0
N1
N2
N1d
N1w
N2w
N4w
N13M
N3M
N6M

# v1.17.0: Upstream OpenAPI spec removed the EdgeLog* types
# (log parser patterns and log templates were dataset settings that have
# since been reworked). No in-package shim is possible.
EdgeLogParserPattern
EdgeLogTemplate
EdgeLogTemplateFieldType
SettingsPerOrganizationAndDatasetInfo.LogParserPatterns
SettingsPerOrganizationAndDatasetInfo.LogTemplates

# v1.17.0: Upstream spec change on
# TimeSeriesAggregationMetricNameMatch.OtherFilters — from a
# []AttributeFilter list to a *FilterCriteria pointer (unifies on the
# richer criteria shape used elsewhere).
TimeSeriesAggregationMetricNameMatch.OtherFilters

# v1.21.0: Upstream OpenAPI spec replaced the inline
# `external`/`builtin`/`userdefined` enum on ViewLabels.dash0.com/source with a
# $ref to the shared CrdSource enum, so oapi-codegen no longer emits the
# per-field ViewLabelsDash0Comsource type or its constants. Callers move to
# CrdSource; note the server now reports view provenance with the CrdSource
# values (ui, terraform, operator, dash0-cli, platform, api) rather than the
# three former view-only values.
ViewLabelsDash0Comsource
Builtin
External
Userdefined

# v1.21.0: The same regen added a second `disabled`/`adaptive` enum to the
# spec, so oapi-codegen disambiguated the SamplingMode constants and the bare
# Adaptive and Disabled constants generated.go used to export are gone.
# gorelease still flags the removal because it diffs generated.go directly,
# but compat.go re-declares both names as deprecated aliases for
# SamplingModeAdaptive/SamplingModeDisabled, so consumers do not break.
Adaptive
Disabled

# v1.21.0: gorelease reports these two union accessors as changed while printing
# an identical before-and-after signature
# (func(SlackTriggerVariableName) error). Neither the methods, their receiver
# type, nor SlackTriggerVariableName itself differ in the regenerated file, and
# the flag does not reproduce outside CI, so this is a gorelease artifact of the
# sibling SlackBotFailedCheck union item being added rather than a real change.
(*SlackBotReactionAgenticWorkflowTriggerSpec_AvailableVariables_Item).FromSlackTriggerVariableName
(*SlackBotReactionAgenticWorkflowTriggerSpec_AvailableVariables_Item).MergeSlackTriggerVariableName
